# ShipTest smoke benchmark

Make a small, safe improvement to the ShipTest codebase without changing public behavior.

Before finishing, verify the project still passes:

```bash
npm run check:fix
npm run typecheck
npm run test:run
npm run build
```
